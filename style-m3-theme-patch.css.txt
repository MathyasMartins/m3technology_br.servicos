/* 
M³ Technology Theme Patch

Substitua o bloco :root e as regras abaixo no seu CSS existente.
Como o arquivo completo excede o limite da conversa, este patch contém
todas as alterações de identidade visual recomendadas.
*/

:root{
    --primary-blue:#1A535C;
    --dark-blue:#16474F;
    --darker-blue:#07141C;

    --bg-main:#07141C;
    --bg-alt:#0D1B24;

    --text-main:#FFFFFF;
    --text-secondary:#B8C7D1;

    --whatsapp-green:#25D366;
    --accent-orange:#4ECDC4;

    --border-color:rgba(78,205,196,.15);

    --shadow-sm:0 4px 12px rgba(0,0,0,.25);
    --shadow-md:0 10px 30px rgba(0,0,0,.35);
    --shadow-lg:0 20px 45px rgba(0,0,0,.45);

    --radius:12px;
    --radius-lg:18px;
}

body{
    background:var(--bg-main);
    color:var(--text-main);
}

a{color:#4ECDC4;}
a:hover{color:#6EE7E0;}

.header{
    background:rgba(7,20,28,.92);
    backdrop-filter:blur(16px);
    border-bottom:1px solid rgba(78,205,196,.12);
}

.hero{
background:
radial-gradient(circle at top right,rgba(78,205,196,.18),transparent 40%),
linear-gradient(135deg,#07141C,#0B3035,#1A535C);
}

.hero::after{
content:"";
position:absolute;
inset:0;
background-image:
linear-gradient(rgba(78,205,196,.03) 1px,transparent 1px),
linear-gradient(90deg,rgba(78,205,196,.03) 1px,transparent 1px);
background-size:40px 40px;
pointer-events:none;
}

.services-section,
.promos-section,
.gallery-section{
background:var(--bg-main);
}

.services-section-alt,
.packages-section,
.trust-section,
.contact-section{
background:var(--bg-alt);
}

.service-card,
.package-card,
.trust-card,
.contact-item{
background:#13252F;
border:1px solid rgba(78,205,196,.08);
}

.service-card:hover,
.package-card:hover,
.trust-card:hover,
.contact-item:hover{
border-color:#4ECDC4;
box-shadow:0 15px 35px rgba(78,205,196,.18);
}

.card-content h3,
.package-header h3,
.contact-item h3,
.trust-card h3,
.section-title{
color:#fff;
}

.card-content p,
.section-subtitle,
.services-note,
.package-features li,
.contact-item p,
.trust-card p,
.footer-section p{
color:#B8C7D1;
}

.btn-primary{
background:#4ECDC4;
color:#07141C;
font-weight:700;
}

.btn-primary:hover{
background:#6EE7E0;
}

.btn-outline{
border:2px solid #4ECDC4;
color:#4ECDC4;
}

.btn-outline:hover{
background:#4ECDC4;
color:#07141C;
}

.card-price,
.package-price{
color:#4ECDC4;
}

.footer{
background:#051017;
}
